/**
 * Blueprint alias — same implementation as ReportEditorBlock (editable narrative with optional autosave).
 */
export { ReportEditorBlock as ReportViewerEditorBlock } from '@/components/analysis/report-editor-block'
